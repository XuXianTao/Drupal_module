captcha.generate:
  path: 'api/webform/captcha/generate/{webform_id}'
  defaults:
    _controller: 'Drupal\webform_rest_resource\Controller\CaptchaController::generate'
  requirements:
    _permission: 'access captcha'
